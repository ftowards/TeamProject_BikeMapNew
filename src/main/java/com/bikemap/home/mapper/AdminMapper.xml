<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bikemap.home.admin.AdminDaoImp">
	
	<!-- admin추가 -->
	<select id="registAllRecord" resultType="com.bikemap.home.regist.RegistVO">
		select rownum, a.userid, a.username, a.gender, a.birth, a.tourcnt, a.heart, a.report, b.endday
		from regist a join suspend b on a.userid = b.userid order by rownum desc
	</select>
	
</mapper> 